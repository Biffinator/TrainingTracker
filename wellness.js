import {plan,resolveLongDay} from './core.js?v=3.17.2';
export const isExercise=t=>!/^cold plunge/i.test(t.n);
export function daySummary(db,d){const r=db.days[d]||{},tasks=plan(d,db.start,db.days,resolveLongDay(db,d)).filter(isExercise);let minutes=0,load=0,rated=0,logged=0,cardio=0;const entries=[...tasks.map(t=>({task:t,session:r.sessions?.[t.id]})),...(r.archivedTasks||[]).slice().reverse()];for(const entry of entries){const t=entry.task;if(!isExercise(t))continue;const m=entry.session;if(m?.minutes!==undefined&&m.minutes!==''&&+m.minutes>0){minutes+=+m.minutes;if(!t.lift)cardio+=+m.minutes;logged++;if(m.rpe!==undefined&&m.rpe!==''){load+=+m.minutes*+m.rpe;rated++;}}}return {date:d,planned:tasks.length,completed:tasks.filter(t=>r.done?.[t.id]).length,minutes,cardio,load,rated,logged,hard:tasks.filter(t=>t.lift||/long run|long bike|tempo|4×4|basketball/i.test(t.n)).length};}
